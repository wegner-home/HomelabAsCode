---
# Host vars for localhost

ansible_become_pass: '{{ lookup(''community.sops.sops'', ''{{ project_root }}/secrets/secrets.sops.yaml'', extract=''["vault_secrets"]["localhost"]["sudo_password"]'') }}'


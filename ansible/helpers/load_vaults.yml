---
# DEPRECATED
# Secrets are now resolved via direct community.sops.sops lookups in vars/defaults.
# This helper is intentionally a no-op for backward compatibility.

- name: load_vaults helper is deprecated (no-op)
  ansible.builtin.debug:
    msg: 'helpers/load_vaults.yml is deprecated and no longer needed.'
  changed_when: false
  tags: ['always']

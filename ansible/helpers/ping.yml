---
# Simple ping playbook to test connectivity to all hosts
- name: Ping all hosts
  hosts: all
  gather_facts: false

  tasks:
    - name: Ping hosts
      ansible.builtin.ping:

    - name: Display success message
      ansible.builtin.debug:
        msg: 'Successfully connected to {{ inventory_hostname }}'

---
galaxy_info:
  author: Philipp Wegner
  description: >-
    Orchestrator role — delegates to atomic child roles for Terraform
    generation, NixOS flakes, Docker Compose repos, and K8s GitOps repos.
    Provides shared project settings (dir, owner, group, env) and
    build-target booleans.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
    - name: GenericLinux
      versions:
        - all

dependencies: []

# SOPS secrets file - Generated from Ansible Vault
# Host: {{ inventory_hostname }}
# Role: {{ host_role | default('unknown') }}
# Generated: {{ ansible_date_time.iso8601 | default('N/A') }}
#
# These secrets are populated from Ansible vault (vault takes precedence)
# Encrypted with SOPS/age for NixOS sops-nix module

{% for secret_name, secret_value in _host_secrets.items() %}
{% if secret_value | length > 0 %}
{{ secret_name }}: {{ secret_value | to_json }}
{% else %}
# {{ secret_name }}: (empty - not set in vault)
{% endif %}
{% endfor %}

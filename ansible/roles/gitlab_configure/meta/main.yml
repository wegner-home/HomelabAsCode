---
galaxy_info:
  author: Philipp Wegner
  description: >-
    GitLab orchestrator — credential generation, service startup waits,
    and validation. Delegates API bootstrap to gitlab_api_bootstrap
    and repository initialization to gitlab_repo_init.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
    - name: GenericLinux
      versions:
        - all

dependencies:
  - role: sops_write

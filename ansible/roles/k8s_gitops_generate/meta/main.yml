---
galaxy_info:
  author: phil
  description: >-
    Generates a FluxCD GitOps repository structure from Ansible inventory
    using the blueprint_repo role. Renders cluster manifests for Cilium,
    MetalLB, kube-vip, cert-manager, Traefik, and SOPS secrets operator
    via the blueprints/k8s/ blueprint.
  license: MIT
  min_ansible_version: '2.14'

dependencies: []
